# Routes

This folder defines all application routes.

## Examples

- AppRoutes.jsx → main route configuration
- ProtectedRoute.jsx → restrict access based on authentication
- RoleRoute.jsx → restrict access based on user roles (admin/user)

## Purpose

To centralize navigation logic and control which pages users can access.