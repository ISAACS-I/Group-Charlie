# Context

This folder contains React Context files for global state management.

## Examples

- AuthContext → handles user authentication and roles
- EventContext → manages events, bookings, and registrations

## Purpose

To share data (like user info or events) across multiple components without prop drilling.