# Config

This folder contains configuration files.

## Examples

- sidebarConfig.js → role-based sidebar links
- routesConfig.js → application routes

## Purpose

To centralize configuration and avoid hardcoding values in components.