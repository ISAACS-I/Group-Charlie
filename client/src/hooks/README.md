# Hooks

This folder contains custom React hooks.

## Examples

- useAuth.js → handles authentication logic
- useFetch.js → reusable data fetching hook
- useLocalStorage.js → sync state with local storage

## Purpose

To reuse logic across components and keep code clean.